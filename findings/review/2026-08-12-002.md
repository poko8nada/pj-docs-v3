---
date: 2026-08-12
outcomes:
  - id: C1
    adopted: true
    note: 修正済み。git diff <baseCommit> -- <rel>（コミット対作業ツリー比較）に変更
  - id: C2
    adopted: true
    note: 修正済み。rel を posix 形式に正規化してから分割
  - id: M1
    adopted: false
    note: truncate 重複は対応保留
  - id: M2
    adopted: true
    note: 修正済み。baseCommit 分岐と git 失敗時のテスト2件を追加
  - id: M3
    adopted: true
    note: 修正済み。formatTargets のみ経路と未インストール時のテストを追加
---

## R-what: Target

- Target: .opencode/plugins/lib/checks/quality.ts, .opencode/plugins/lib/checks/quality.test.ts, .opencode/plugins/lib/checks/drift.ts, .opencode/plugins/lib/checks/drift.test.ts
- Background: 旧 quality-gate.ts（削除予定）を lib/checks/ に再構成した。コミット前レビュー。
- Constraints: 対象は untracked の新規ファイルなので git diff には現れない。read で直接読む。前回調査の所見 (1) checkTargets 空ガード (2) cumulativeAdditions のパス正規化 (3) fakeRun の検証十分性 を確定させる。

## Check (ALL three viewpoints, in order)

- Correctness
  - ID: C1
  - Severity: medium
  - Finding: drift.ts の cumulativeAdditions は baseCommit 存在時に git diff --numstat <baseCommit>..HEAD の two-dot range を使うが、git diff の two-dot は「コミット対コミット」の比較であり、staged / unstaged（作業ツリー）の変更を一切含まない。driftCheck は session.idle 時に実行されるため、ベースライン（baseCommit）が存在する通常フローでは「現在セッションの未コミット追加行」が常に 0 となり、driftSuspected が実質発火しない。これはコメント「baseCommit 以降の累積追加行数」および .opencode/skills/charter/references/header-format.md の "cumulative additions since the baseline commit" という仕様に反する。
  - Recommendation: baseCommit 存在時は git diff --numstat <baseCommit> -- <rel>（コミット対作業ツリー比較。baseCommit 以降のコミット済み + staged + unstaged を全て含む）に変更する。baseCommit が無い場合の git diff HEAD は現状のまま未コミット分のみで仕様通り。
  - Source: .opencode/plugins/lib/checks/drift.ts:125-127
  - ID: C2
  - Severity: low
  - Finding: isCodeFile は rel.split('/') でパスを分割するが、Windows では path.relative がバックスラッシュ区切りを返すため、segments[0] が ALLOWED_DIRS に一致せず、サブディレクトリのコードファイルが全てドリフト対象外になる（サイレント無効化）。現環境は darwin のため発現しない潜在バグ。
  - Recommendation: rel.split(path.sep) を使うか、rel を path.posix 形式に正規化してから分割する。
  - Source: .opencode/plugins/lib/checks/drift.ts:61,65
  - 所見 (1) の確定: quality.ts の 148 行目の早期 return と 175 行目のガードは両方とも必要で、関係は妥当。148 行目は fix / format を含む全作業をスキップする高速パスであり、175 行目は formatTargets のみ存在するケース（例: .md のみ編集）で lint / typecheck を実行させないための保護。空配列のまま oxlint を実行すると引数なし扱いでプロジェクト全体を lint してしまうため、旧コードで lintFiles 内部にあった空配列ガードが削除された現在は 175 行目が唯一の保護であり、配置も lint / typecheck 実行（180-181 行目）の直前で正しい。
  - Source: .opencode/plugins/lib/checks/quality.ts:148,175
- Security
  - no findings
- Maintainability
  - ID: M1
  - Severity: low
  - Finding: truncate 関数と MAX_OUTPUT_LINES 定数が quality.ts と drift.ts に完全に同一実装で重複している。今後のチェック追加時にさらに複製される恐れがある。
  - Recommendation: 共通の lib モジュール（例: lib/checks/truncate.ts）に抽出して両チェックから import する。
  - Source: .opencode/plugins/lib/checks/quality.ts:42,51-60 / .opencode/plugins/lib/checks/drift.ts:46,113-122
  - ID: M2
  - Severity: medium
  - Finding: drift.test.ts の fakeRun は cmd[0] === 'git' しか検証せず、-- パス区切り・rel パス・範囲引数を一切検証しない。さらに全テストの STATUS は baseLines のみで baseCommit を含まず、base = ['HEAD'] の分岐しか通らないため、cumulativeAdditions の baseCommit 分岐（drift.ts:126 の ..HEAD 生成）が完全に未テスト。これが上記の two-dot range バグをテストが検出できない直接の原因になっている。
  - Recommendation: baseCommit を STATUS に含むケースを追加し、fakeRun 内で cmd 配列に baseCommit と ..HEAD（または修正後の形式）・-- ・rel パスが渡ることを expect する。git diff が非ゼロ exitCode を返すケース（additions null → driftSuspected false）も追加する。
  - Source: .opencode/plugins/lib/checks/drift.test.ts:19-26,29-45
  - ID: M3
  - Severity: medium
  - Finding: quality.test.ts は formatTargets のみ存在する経路（checkTargets 空 → quality.ts:175 のガード）をテストしておらず、「ツール未インストール」テストも run が呼ばれないことを検証していない。run スタブが良性の出力を返すため、175 行目のガードが壊れて oxlint が引数なしで実行される回帰が起きても全テストが green のまま通過する。
  - Recommendation: .md のみを編集したケースを追加して oxlint / tsc が呼ばれないことをスパイで検証し、未インストールテストでも run の呼び出し回数 0 を assert する。あわせて fakeRun で --fix の有無・対象ファイル配列・tsc の -p <tmp> --noEmit などの引数形状を検証する。
  - Source: .opencode/plugins/lib/checks/quality.test.ts:72-89 / .opencode/plugins/lib/checks/quality.ts:173-181
  - 所見 (2) の確定: cumulativeAdditions のパス指定は既に正規化済み。detectDrift が path.relative(ctx.root, abs) で算出した rel（drift.ts:143）をそのまま渡しており、git は cwd=ctx.root で実行されるため絶対パスによる不整合は発生しない。path.relative の追加は不要。
  - Source: .opencode/plugins/lib/checks/drift.ts:143,167
  - 所見 (3) の確定: fakeRun は「出力からフォローアップ生成」という挙動検証には十分だが、「実コマンドの引数検証」には不十分。上記の通り、git の範囲引数と pathspec、quality の --fix / ファイル配列 / -p tmp が検証対象外で、実際に two-dot range バグが素通りする状態。
  - Source: .opencode/plugins/lib/checks/drift.test.ts:19-26 / .opencode/plugins/lib/checks/quality.test.ts:23-36

## A-verdict: Assessment

- Verdict: PASS
- Reason: high 相当の不具合は無し。ただし baseCommit 存在時の ..HEAD は driftSuspected を通常フローで実質無効化する論理バグ（medium）であり、テストがそれを検出できない構造（medium）と合わせ、マージ前に cumulativeAdditions の修正と baseCommit 分岐テストの追加を推奨する。ガード (1) とパス正規化 (2) は妥当と確定。
