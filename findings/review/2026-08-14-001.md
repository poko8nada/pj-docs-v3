---
date: 2026-08-14
outcomes:
  - id: C1
    adopted: true
    note: --format=agent を明示追加し出力形式を固定。実機検証で数え上げが機能することも確認
  - id: C2
    adopted: true
    note: CODE_EXTENSIONS に mts/cts を追加し、テストにアサートを追加
  - id: C3
    adopted: true
    note: 非零 exit + 数え上げ0 を失敗扱いに。opencode quality.ts にも同修正を両適用
  - id: M1
    adopted: false
    note: メッセージ文面の乖離は loop_limit 自動再実行の文脈で実害なしと判断し reject
  - id: M2
    adopted: false
    note: テスト細部は adopt 分（C2/C3 のテスト）で必要な分は固定済みと判断し reject
---

## R-what: Target

- Target: .cursor/hooks/handlers/verify.mjs, .cursor/hooks/handlers/verify.test.mjs, .cursor/hooks/lib/file-extensions.mjs, .cursor/hooks/lib/file-extensions.test.mjs (all in /Users/yuyahanada/Desktop/main/_pj-docs-v3)
- Background: Cursor harness の stop-hook 品質チェックを opencode の idle-chain 品質チェック (.opencode/plugins/lib/checks/quality.ts) に揃える変更。pnpm スクリプトから node_modules/.bin の直接呼び出しへ、失敗判定を exit code から oxlint 出力の warning/error 数え上げへ、format:check 廃止、TYPECHECK_EXTENSIONS の全 JS/TS 系拡張子化
- Constraints: opencode quality.ts と同じ判定意味論になっているか、warning/error 数え上げのエッジケース、touchedPaths の相対/絶対パス処理、stop-hook の loop_limit 意味論、テストが変更挙動をカバーしているかをレビュー。対象外ファイルはレビューしない

## Check (ALL three viewpoints, in order)

- Correctness
  - ID: C1
  - Severity: high
  - Finding: verify.mjs:108-109 の数え上げ正規表現 `: warning ` / `: error ` は oxlint 1.78.0 のデフォルト出力形式と一致しない。公式ドキュメント (oxc.rs/docs/guide/usage/linter/output-formats.html) および oxc リポジトリのスナップショット (PR #19590, 2026-02) によると、デフォルトフォーマットは `x eslint(rule): msg` のフレーム表示と `Found N warnings and M errors.` のサマリー行で、`path:line:col: error rule: msg` 形式は `--format=agent` でのみ出力される。そのため warnings + errors は常に 0 となり、lint の問題が一切 followup にならず、旧実装の exit code 判定からの回帰となる。テストは旧形式をモックしているためこの不整合を検出できない
  - Recommendation: runChecks の oxlint 呼び出し (verify.mjs:105) に `--format=agent` を追加して判定を実出力に合わせる。テストのモックも agent 形式の実サンプルに更新し、本物の oxlint 出力を前提にした検証を入れる
  - Source: .cursor/hooks/handlers/verify.mjs:105
  - ID: C2
  - Severity: medium
  - Finding: file-extensions.mjs:8 の CODE_EXTENSIONS は ['js','jsx','ts','tsx','mjs','cjs'] で 'mts' と 'cts' が欠落しており、TYPECHECK_EXTENSIONS と OXLINT_EXTENSIONS の両方が .mts/.cts を対象外とする。quality.ts:38 の CHECKABLE_EXT は 8 拡張子 (.ts/.tsx/.mts/.cts/.js/.jsx/.mjs/.cjs) を含み、変更意図 (ts/tsx/mts/cts/js/jsx/mjs/cjs) やコメント「opencode の CHECKABLE_EXT に合わせる」とも食い違う。.mts/.cts ファイルは lint も typecheck もスキップされる
  - Recommendation: CODE_EXTENSIONS に 'mts' と 'cts' を追加し、file-extensions.test.mjs に .mts/.cts のアサーションを追加する
  - Source: .cursor/hooks/lib/file-extensions.mjs:8
  - ID: C3
  - Severity: low
  - Finding: lint の失敗判定は exit code を見ず数え上げのみで行うため、oxlint がクラッシュや起動失敗 (非零 exit, 一致する出力なし) した場合にクリーン扱いで通過する。旧実装は非零 exit で失敗にしていた。quality.ts と同じ挙動ではあるが、ツール障害が静かに見逃されるリスクがある
  - Recommendation: lint.ok が false かつ警告/エラー数が 0 のときはフォールバックで失敗扱いにするか、ログに警告を出す
  - Source: .cursor/hooks/handlers/verify.mjs:110
- Security
  - 指摘なし。execFile は args 配列でコマンド実行しておりシェルインジェクションの余地はなく、touchedPaths はプロジェクト内の実在ファイルにフィルタ済みで、bin パスも固定構築のため問題なし
- Maintainability
  - ID: M1
  - Severity: low
  - Finding: typecheck 失敗メッセージが quality.ts と乖離している。ポートは formatFailure で `## typecheck (exit N)` と出力しエラー数を出さない (quality.ts は `## Typecheck (tsc): N error(s)`)。また followup 冒頭の「Fix them and verify again.」は警告も自動修正を指示する文面で、quality.ts の「warnings はユーザーに提案せよ」という指示と矛盾する
  - Recommendation: メッセージ形式を quality.ts に合わせてエラー数を表示し、警告への対応方針を followup に含める
  - Source: .cursor/hooks/handlers/verify.mjs:129
  - ID: M2
  - Severity: low
  - Finding: 変更挙動のテストカバレッジに穴がある。エラーと警告の混在カウント、stderr 経由の診断、getMissingVerifyTooling の欠落分岐、format 対象のみ触れた場合のフローが未テスト。file-extensions.test.mjs は TYPECHECK_EXTENSIONS で js/jsx/mts/cts を検証しておらず、今回の拡張意図を部分的にしか固定しない。さらに verify.test.mjs は run-command.mjs しかモックせず tooling.mjs は実ファイルシステムに依存するため、node_modules が無い環境で全テストが欠落ツーリング分岐に倒れる脆弱性がある
  - Recommendation: 混在カウント・stderr・欠落ツーリング・format のみ対象のテストを追加し、TYPECHECK_EXTENSIONS の全 8 拡張子をアサートし、tooling.mjs もモックして環境非依存にする
  - Source: .cursor/hooks/handlers/verify.test.mjs:48

## A-verdict: Assessment

- Verdict: FAIL
- Reason: C1 (high) により、変更の主目的である lint 問題の検出が実環境の oxlint 出力では機能せず、warning/error 数え上げが常に 0 になるため、マージしたままでは lint チェックが死んでいる状態になる
