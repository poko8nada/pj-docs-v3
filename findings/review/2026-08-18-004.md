---
date: 2026-08-18
---

## R-what: Target

- Target: .opencode/skills/skill-sync/scripts/sync.mjs, .opencode/skills/product/scripts/check-complete.mjs, .opencode/skills/charter/scripts/list-headers.mjs
- Background: プロジェクト共通定数を constants/index.mjs に集約する移行の一部。skill-sync は SKIP_DIRS、check-complete は PRODUCTS_DIR、list-headers は CODE_EXTENSIONS / ALLOWED_DIRS / SKIP_DIRS を import に移行
- Constraints: レビュー対象は上記3ファイルの変更のみ。移行の機械的正確性、check-complete の PRODUCTS_DIR_ABS 解決、list-headers の ALLOWED_DIRS 挙動変化、import パス、3ハーネス間の同一性を検証

## Check (ALL three viewpoints, in order)

- Correctness
  - <no findings>
- Security
  - <no findings>
- Maintainability
  - <no findings>

## A-verdict: Assessment

- Verdict: PASS
- Reason: 移行は機械的に正確。ローカル定数は全て削除され、参照は全て import 先の変数名に更新されている。check-complete.mjs の PRODUCTS_DIR_ABS は `path.resolve(dir, '../../../../', PRODUCTS_DIR)` により `PRODUCTS_DIR` = `'products'` をプロジェクトルートに結合し、旧ハードコード `'../../../../products'` と等価である。list-headers.mjs の ALLOWED_DIRS は共有版が `worker`、`.commandcode`、`constants` を追加包含しており、これは意図的なドリフト解消である。import パス `../../../../constants/index.mjs` は全3スクリプトでスクリプト位置から4階層上がプロジェクトルートであり正しい。.commandcode と .cursor のコピーは diff 結果空であり .opencode 版と完全に同一。skill-sync は .commandcode / .cursor に存在しないため該当なし。問題は確認されなかった
