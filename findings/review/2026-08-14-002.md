---
date: 2026-08-14
outcomes:
  - id: C1
    adopted: true
    note: drift.run は no-op 時に undefined を返すよう修正。chain テストで verify の followup 保全を固定
  - id: C2
    adopted: true
    note: isRegularFile ガードを追加し、ディレクトリ混入時に throw しないよう修正
  - id: M1
    adopted: true
    note: 相対パス・HEAD 分岐・isDone:false 除外・incomplete header・書換アサート・ディレクトリ無視のテストを追加
  - id: M2
    adopted: false
    note: MACHINE_PREFIX はドリフトメッセージが次ターンの agent 向けのため誤認リスク低と判断し reject
  - id: M3
    adopted: false
    note: Cursor の loop_limit が既に有界。ドリフトの再発火は本来の挙動のため reject
  - id: M4
    adopted: false
    note: ログ観測用途の文言であり実害なし。優先度低と判断し reject
---

## R-what: Target

- Target: .cursor/hooks/handlers/drift.mjs, .cursor/hooks/handlers/drift.test.mjs, .cursor/hooks/runtime/chain.mjs, .cursor/hooks/runtime/chain.test.mjs
- Background: opencode の drift-gate (.opencode/plugins/lib/checks/drift.ts) を Cursor stop フックへ移植した。サイズ・ドリフト検出で STATUS を書き換え、ヘッダー欠落を followup_message で案内する。stop チェーンは [verify, drift] となり drift は整形済みの確定状態を読む。
- Constraints: 移植の忠実性（閾値・ベースライン解析・git diff --numstat の意味論・ヘッダー解析正規表現・truncate・メッセージ形式）、相対 touchedPaths のパス解決、git diff の意味論（baseCommit vs HEAD）、テスト網羅性のみをレビューする。対象外ファイルはレビューしない。

## Check (ALL three viewpoints, in order)

- Correctness
  - ID: C1
  - Severity: high
  - Finding: runChain のレスポンス合成は「後勝ち」（result.response !== undefined で上書き）なのに、drift.run は何も検出しない場合に { response: {} } を返すため、常に verify の followup_message を消してしまう。verify が lint/typecheck 失敗を検出しても drift が no-op なら最終レスポンスは {} になり、Cursor はループを終了して品質ゲートの指摘が一切伝わらない。drift がフラグを立てた場合も verify のメッセージはドリフト用メッセージで上書きされ、両者の指摘が同時に伝わることはない。
  - Recommendation: drift.run は followUps が空（files が空も含む）の場合に undefined を返して verify のレスポンスを生かす（format.mjs の規約に合わせる）。両ハンドラの指摘を併記する場合は runChain で followup_message を結合する方式に変更する。stop チェーンで verify の followup が drift の no-op に潰されないことをテストで固定する。
  - Source: .cursor/hooks/runtime/chain.mjs:62, .cursor/hooks/handlers/drift.mjs:48, .cursor/hooks/handlers/drift.mjs:70
  - ID: C2
  - Severity: medium
  - Finding: parseHeader が readFileSync を isFile ガードなしに呼ぶため、touchedPaths にディレクトリや読み取り不能なパスが混入すると EISDIR/EACCES で throw し、チェーン全体が失敗する。main.mjs の failSafe が { decision: 'allow' } を返すためエラーは stderr にのみ出て、drift の STATUS 書込は残る一方で verify の followup は失われ、ゲートが静かに無効化される。snapshot の touchedPaths はイベント由来で広めの集合のため、opencode（編集ファイルのみ）より混入リスクが高い。
  - Recommendation: parseHeader 呼び出し前に statSync(abs).isFile() を確認するか、parseHeader を try/catch で包み失敗時は null を返す。テストにディレクトリパス混入ケースを追加する。
  - Source: .cursor/hooks/handlers/drift.mjs:125, .cursor/hooks/handlers/drift.mjs:182, .cursor/hooks/handlers/drift.mjs:229
- Security
  - no findings
  - パスは execFile の args 配列で渡すため shell 注入の余地はなく、runCommand の失敗も null 扱いで安全側に倒れる。
- Maintainability
  - ID: M1
  - Severity: medium
  - Finding: テスト網羅に穴がある。相対 touchedPaths の分岐（isAbsolute が false で join する経路）は全テストが絶対パスを渡すため未検証、C1 のレスポンス保全（verify の followup が drift no-op で潰されないこと）は未検証、baseCommit なしで git diff HEAD を実行する分岐は未検証、isDone: false 除外（drift.mjs:190）は未検証、FEATURES/PURPOSE 欠落の incomplete header は未検証、truncate の 40 行超えは未検証。driftSuspected テストはファイル書換を検証せず followup_message のみ確認する。
  - Recommendation: 相対パス・HEAD 分岐・isDone: false 除外・incomplete header・truncate のテストを追加し、driftSuspected テストで STATUS 行の書換をアサートする。
  - Source: .cursor/hooks/handlers/drift.test.mjs:28, .cursor/hooks/handlers/drift.test.mjs:53
  - ID: M2
  - Severity: low
  - Finding: drift の followup_message に verify.mjs が使う MACHINE_PREFIX（[Automated hook message — not from the user]）がない。Cursor は stop フックの followup_message を次のユーザーメッセージとして自動送信するため、drift の自動メッセージだけ「ユーザー発言」として誤認されやすく、verify との表記規約も不整合になる。
  - Recommendation: verify.mjs と同様の MACHINE_PREFIX を buildHeaderIssuesMessage / buildDriftMessage の冒頭に付与する。
  - Source: .cursor/hooks/handlers/drift.mjs:243, .cursor/hooks/handlers/drift.mjs:254, .cursor/hooks/handlers/verify.mjs:21
  - ID: M3
  - Severity: low
  - Finding: drift は verify のような loop_count ガードを持たず、ヘッダー欠落などの解消されない状態では毎回 followup_message を返し続ける。Cursor の loop_limit（hooks.json で 3）により有界だが、verify は loopLimit 到達で {} を返すのに drift は返し続けるため、動作が不整合で意図が読み取りにくい。
  - Recommendation: loop_count が loopLimit 以上なら no-op を返すガードを verify と同じ方式で追加するか、ドリフトの再発火が意図的であることをコメントで明示する。
  - Source: .cursor/hooks/handlers/drift.mjs:46, .cursor/hooks/handlers/verify.mjs:34
  - ID: M4
  - Severity: low
  - Finding: log.reason が常に "drift flags updated: N file(s), header issues: M" の形式で、フラグ更新が 0 でも header issues のみのケースを「flags updated」と表現する。ログの意味が実態とずれる。
  - Recommendation: 検出数とヘッダー問題数を分けて表現するなど、実態に合わせた reason を組み立てる。
  - Source: .cursor/hooks/handlers/drift.mjs:78

## A-verdict: Assessment

- Verdict: FAIL
- Reason: C1 により stop チェーンの verify 品質ゲートが実質無効化されるため修正必須と判断。なお、背景の「isDone-false 除外がない」という指摘は誤りで、drift.mjs:190 に opencode と同一の除外（PURPOSE に (isDone: true) が無ければ null を返す）が実装済みである。移植のロジック（SIZE_DRIFT_MAX_LINES=300 / DRIFT_SUSPECTED_RATIO=0.5 / DRIFT_SUSPECTED_MIN_LINES=100、baseLines/baseCommit 解析、git diff --numstat <base> -- <rel> の HEAD vs 作業ツリー比較、ヘッダー正規表現、truncate、メッセージ文面）は drift.ts と逐語一致しており、相対パス解決と runCommand（ok 判定）への置換も正しい。
